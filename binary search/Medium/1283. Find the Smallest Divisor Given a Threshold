
    int smallestDivisor(vector<int>& nums, int threshold) {
        int left = 1;
        int right = *max_element(nums.begin(), nums.end());
        int result = right;

        while (left <= right) {
            int mid = left + (right - left) / 2;
            int sum = 0;

            for (int num : nums) {
                sum += (num + mid - 1) / mid;  // ceil division
            }

            if (sum <= threshold) {
                result = mid;
                right = mid - 1;  // try smaller divisor
            } else {
                left = mid + 1;   // need a bigger divisor
            }
        }

        return result;
    }


//remember that it is similar to koko eating bananas as over there she had to finish within the hous and here it is
the threshold
